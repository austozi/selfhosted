# Watchtower

[Watchtower](https://github.com/containrrr/watchtower) is a utility that monitors Docker images for updates. It can also be used to automate Docker image updates.

## Install

Run `docker-compose up -d -f docker-compose.yml`. 

There is no frontend to expose.
