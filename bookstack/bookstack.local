# /path/to/swag/config/fail2ban/filter.d/bookstack.local

[INCLUDES]
before = common.conf

[Definition]
failregex = ^.*\"PHP message: Failed login for .+\" while reading response header from upstream, client: <ADDR>.*$
ignoreregex =
